# Phase 9 — Scale, Robustness, and Model Economics

**Status:** planned · **Depends on:** Phase 5 (loop architecture); pairs with
Phases 6–8 (9.2 before Phase 8's longest runs; 9.5 helps all) ·
**Read first:** [Phase5.md](Phase5.md) §0 · [styleguide.md](styleguide.md)

> **Replan note (2026-09-22, post-decisions):** streaming was **rejected
> for now** (decision #2) — step 9.1 is now the F2 stall watchdog, and the
> F2 prompt half lives in `Phase7.md` step 7.1. Step 9.4 now carries the
> full F3 multi-provider design (+ the D5 badge/token row). Streaming and
> the O2 char-estimate stay conditional (§4). Step numbers are unchanged.

---

## 1. Introduction — what this phase delivers

The phases so far add capability; this one adds **stamina**. The v1 audit's
top limitations — session-only chats, hard context dead-ends, one model for
everything, silent stalls, no stuck-model economics — all land here. None of
it is glamorous; all of it decides whether a 40-round build session survives.

Concretely:

1. **Waiting UX: the F2 stall watchdog** — decision #2 (2026-09-22) said
   **no streaming for now**; the approved replacement is prompt-side
   progress discipline (landed in `Phase7.md` step 7.1: obligatory to-dos
   via `create_or_update_plan` + a one-sentence update per completed
   item) plus an orchestrator-side watchdog that notices silent stalls in
   an in-flight turn and tells the user **in-chat**. Streaming stays
   deferred (§4) — revisited only if latency is still a complaint after
   F2 lands.
2. **Context compaction** — at ~75% window: summarize the oldest half of the
   transcript through the user's own endpoint, keep a preserved block
   (design intent, open problems, object map, loaded skills, latest
   screenshots' *text* siblings), drop old tool outputs and **all old
   images** first. The `byok-context-full` dead-end becomes a recoverable
   event.
3. **Durable chat history** — owner-designed (decision #1, 2026-09-22):
   chats saved to human-readable files (YAML/Markdown) after every user
   message, when the AI finishes, and on app closure; clear window on
   reopen with old chats loadable from a chat-tab history button; names =
   first 5 words of the first prompt + date of last interaction (details in
   step 9.3).
4. **Multi-provider routing + per-chat overrides (F3 + D5)** — the
   owner-designed surface (decision #6): users register providers
   (name + endpoint + key); the chat header gets a model dropdown
   (`provider name/model name`, pulled from each provider's `/models`)
   and an effort dropdown (low/medium/high, or the server-listed
   per-model levels), plus the approved D5 badge and exact token row.
   The fast/strong profiles remain as the routing defaults mechanism
   (fast for scout/summary/compaction calls, strong for edits/generation
   — the sub-agent economics pattern), with advanced fields
   (`temperature`, `max_tokens`).
5. **Capability gating + built-in mini-benchmark** — probe and remember what
   the endpoint supports (vision, `strict` schemas, `parallel_tool_calls`,
   `reasoning_effort`, server-listed effort levels); a 4-task built-in
   benchmark so users can
   see which of their models actually builds GDevelop games well (model
   quality dominates every other design choice — SOTA finding).
6. **Local suggestions & feedback** — follow-up suggestion chips generated by
   the user's endpoint (parity with hosted UX); thumbs up/down stored locally
   as a prompt-iteration signal.
7. **Retry/robustness polish** — remember the degraded `reasoning_effort`
   state per model (stop re-400-ing every turn), honor `Retry-After`,
   per-request timeout override for long-reasoning models, mid-run snapshot
   refresh (per-turn `getProjectUserContent` re-read — finishing what Phase
   5's getters started).
8. **Dev-only eval harness** — a fixture suite of ~30 real game tasks with
   reference-outcome scoring, run on demand against any configured endpoint;
   the instrument that tells us whether prompt/skill/tool changes actually
   help (eval-driven tool development is the single most-repeated SOTA
   practice).

### New files created in this phase

```
Byok\ByokWatchdog.js / .spec.js               in-flight stall watchdog + in-chat notices (F2 runtime half)
Byok\ByokCompactor.js / .spec.js              transcript summarization + preserved block + drop order
Byok\ByokChatPersistence.js / .spec.js        IndexedDB store (no new deps; raw IDB wrapper)
Byok\ByokModelRouter.js / .spec.js            provider registry + profiles + routing policy resolution
Byok\ByokCapabilities.js / .spec.js           endpoint capability probe + cache (per model)
Byok\ByokBenchmark.js / .spec.js              4-task built-in benchmark + report
Byok\ByokSuggestions.js / .spec.js            follow-up chips via the user endpoint
scripts\run-byok-evals.js (+ Byok\evals\)     dev-only task suite + scorer (outside shipped bundle)
```

Modified: `Byok\ByokClient.js` (retry polish),
`Byok\ByokOrchestrator.js` (watchdog hook, compaction hook, router,
per-turn snapshot), `Byok\ByokChatStore.js` (persistence delegate),
`Byok\ByokTypes.js`/`ByokSettingsTab.js` (provider registry, profiles,
advanced fields, capability display, benchmark button),
`Byok\ByokKeyStorage.js` (per-provider key slots),
`AiRequestChat\index.js` chat header (F3 dropdowns + D5 badge/token row —
upstream touchpoint, approved #5/#6), `Byok\ByokPrompts.js` (`byok-v7` if
behavior text changes).

---

## 2. Steps

### Step 9.1 — Stall watchdog + in-chat progress notices (F2, decision #2)

**Goal:** a silent hang is never indistinguishable from work. Streaming
was rejected for now (owner decision #2, 2026-09-22); the approved
waiting UX is the Phase 7 prompt discipline (`Phase7.md` step 7.1:
to-dos + per-item progress sentences) plus this watchdog.

**How to implement:**

1. `Byok\ByokWatchdog.js`: while a BYOK turn is in flight, the
   orchestrator reports activity events (request sent, response arrived,
   tool executing, tool output posted, approval requested). The watchdog
   resets a timer on every event; when it expires (default stall window
   90 s, a settings field) it appends an **in-chat** notice — the owner
   explicitly chose in-chat over toasts — e.g. "No activity for 90 s —
   the model may be stuck. You can stop the chat." It is a transcript
   item, not an error row: the existing timeout/error paths already
   produce error rows; this covers only silent stalls between
   activities.
2. Repeat policy: one notice per stall window (no spam); any activity
   ends the stalled state (the notice stays in the transcript as
   history); Stop/suspend/`ready` disarm the watchdog. A single long
   model call is covered by the client timeout (9.7's per-request
   override), not by the watchdog — the watchdog watches gaps, not the
   call itself.
3. Settings: "stall warnings" on/off + the window value in
   `ByokSettingsTab.js`.

**Tests:** no stall → no notice; stall → exactly one notice per window;
activity resets the timer; stop/suspend/ready disarm; toggle off ⇒ never.
**Depends on:** Phase 5 (loop seam); pairs with `Phase7.md` step 7.1.

---

### Step 9.2 — Compaction (`ByokCompactor.js`)

**Goal:** long builds survive the window. **Build this before Phase 8's
flagship long runs.**

**How to implement:**

1. Trigger at `contextUsedRatio ≥ 0.75` (before the model call, not after):
   compact, then proceed. Never trigger mid-tool-batch.
2. Preserved block (rebuilt each compaction from explicit sources, never
   from the model's memory of itself): the chat's project-notes entry
   (Phase 7 tool — the durable design intent), current plan (latest
   `create_or_update_plan` payload), open problems (parse from recent
   failed-tool outputs), object/scene map (a fresh `getSimplifiedProject`
   slice), loaded skills list (bodies reload on demand), latest tool
   verification summary.
3. Drop order (SOTA guidance: redundant tool payloads first, images most
   expensive): (1) all images except the latest one; (2) tool outputs older
   than the last 6 turns → replaced by one-line summaries; (3) user/assistant
   turns older than the last 10 → summarized by **one** extra chat-completions
   call (router picks the fast profile, 9.4) into ≤ 600 tokens.
4. The compacted prefix renders as a single "Context summarized" row in the
   chat UI (transcript item type local to BYOK); `byok-context-full` remains
   only as the guard of last resort when even compaction can't fit (e.g.
   window < 8k).

**Tests:** trigger threshold; preserved block contents sourced correctly;
drop order honored (images first); summarizer call mocked, its output capped;
post-compaction request shrinks (message count/chars asserted); last-resort
error still reachable.
**Depends on:** Phase 5 (loop seam), 9.4 (router) if landed first, else
summarizer uses the main model.

---

### Step 9.3 — Durable history (`ByokChatPersistence.js`)

**Goal:** chats survive restarts; archive means archive.

**Owner design (decision #1, 2026-09-22 — this is the contract):**

- Chats are saved to **file, human-readable: YAML or Markdown** (pick one at
  implementation and record why in the worklog; Markdown renders nicely for
  a future "open chat file" UX, YAML round-trips structured transcripts more
  losslessly — either is accepted).
- **Save points:** after every user message, after the AI finishes
  responding, and on app closure (best-effort flush).
- **Reopen behavior:** the chat window starts **clear**; old chats are
  loaded from a **history button in the chat tab** listing the saved chats.
- **Naming convention:** the first 5 words of the chat's first prompt + the
  date of the last interaction (e.g. `create a forest scene_2026-09-24`).

**How to implement (within the owner design):**

1. File store: desktop writes to a chats folder under the app's user-data
   dir (Electron fs); the web build falls back to IndexedDB persistence of
   the same records (record the choice per platform in the worklog).
   Transcript **images stay id-referenced** (the Phase 6 storage split pays
   off here); image payloads live in per-chat sidecar entries, never inline
   in the text file.
2. `ByokChatStore` delegates through a persistence interface;
   `BYOK_CHAT_PERSISTENCE_ENABLED` flips to `true`; load-on-open is lazy per
   chat (the list reads names/dates only). Corruption quarantine: a file
   that fails shape validation moves aside, never blocks the list.
3. Real archive: `archivedAt` set, excluded from the default list, restorable;
   delete is explicit and separate.
4. History UI: the owner's history button in the chat tab (new small
   component in `Byok\`, listed alongside — not inside — the server-backed
   `AskAiHistory.js`); rename/archive/delete/open actions mirror it.
5. Storage quota: cap total (e.g. 200 MB) with oldest-chats-first eviction of
   *image entries* before any transcript text is lost; a visible usage line
   in settings.

**Tests:** round-trip (create → reload → identical record); quarantine;
archive/restore; quota eviction order; lazy loading never pulls images for
the list; the three save points each produce an up-to-date file.
**Depends on:** Phase 6 (image storage split), Phase 5.

---

### Step 9.4 — Multi-provider model & effort selection (F3) + routing + the D5 badge

**Note (decision #6, 2026-09-22):** implement the owner-designed **F3**
surface from `outofscoped.md` — it supersedes the single-endpoint global
setting as the chat-facing surface (the globals remain as fallback). **D5**
(decision #5) rides the same chat-header work.

**Goal:** cheap reads, strong writes; many providers; the user stays in
control and sees what things cost.

**How to implement:**

1. **Provider registry (settings):** the user registers providers
   (`{id, name, endpoint, keyRef}`) in Preferences → BYOK; each
   provider's key goes through the existing `ByokKeyStorage` (keyed per
   provider id — the Phase 3 safeStorage path is reused unchanged). The
   legacy single endpoint/key migrates into provider #1 on first run.
   "Test connection" works per provider.
2. **Chat dropdown 1 — model:** models are pulled from each provider's
   `GET /models` (the existing models-fetch code, run per provider) and
   listed as `provider name/model name`. The selection is per-chat and
   read at turn time (the getters pattern), overriding profile and
   global settings; the global model remains the fallback for new chats.
3. **Chat dropdown 2 — effort:** defaults to low/medium/high; when the
   server lists per-model supported effort levels (capability probe,
   9.5), those replace the defaults. Per-model context windows continue
   to apply as today.
4. **Routing policy (pure function — F3's defaults mechanism):** with
   multiple providers/models configured, route by call kind — edits/
   generation/event-writing → `strong`; scout sub-agents, compaction
   summaries, suggestion chips, docs/reference summarization → `fast`
   (`fast`/`strong` = model-profile entries over the provider registry).
   Expose "route automatically" vs "always use strong" (single-model
   users are unaffected). Advanced fields per profile: `temperature`
   (unset = provider default), `max_tokens` (unset = omitted; help text
   recommends it for small local models).
5. **D5 — BYOK badge + exact token row in the chat header:** a small
   badge marks the chat as BYOK (naming the active `provider/model`),
   with the exact token counts from `ByokUsageTracker.getTotals` beside
   it (`AiRequestChat` header — upstream touchpoint, approved #5).

**Tests:** provider registry CRUD + migration of the legacy single
endpoint; per-provider key isolation; dropdown resolution order
(per-chat override > profile policy > global fallback); effort-level
source (defaults vs server-listed); policy truth table (call-kind →
profile); unset advanced fields omitted from the request body; badge +
token-row data plumbing.
**Depends on:** Phase 5 getters, Phase 8 (sub-agent call kinds) — policy
ships with the call-kinds that exist at land time.

---

### Step 9.5 — Capability probing + built-in benchmark

**Goal:** no more per-turn 400-dance; users can pick models with evidence.

**How to implement:**

1. `ByokCapabilities.js`: per (endpoint, model) cache in settings:
   - `images` (the Phase 6 auto-detect result, remembered),
   - per-model `effortLevels` (server-listed, when available — feeds the
     9.4 effort dropdown),
   - `reasoning_effort` (remembered degraded state — fixes the audit's
     repeated-400 finding),
   - `strictSchemas`, `parallelToolCalls` (probed lazily on first relevant
     feature use; failures degrade: strict off → validated client-side as
     today; parallel off → `parallel_tool_calls: false` sent).
2. `ByokBenchmark.js` — "Test this model (≈2 min)" button in settings: runs
   4 fixed tasks (create scene+objects; write a working event batch via
   EventScript; fix a deliberately broken event sheet; interpret a fixture
   screenshot if vision on) each scored mechanically (project diff vs
   reference / parse success / assertion), and reports
   pass-count + token cost + rounds used. Results stored per model and shown
   next to the model dropdown. All fixtures ship in `Byok\` (no network).
3. Benchmarks use a scratch in-memory project (never the user's).

**Tests:** capability cache semantics; degradation paths; benchmark scorer
against hand-broken fixture outputs (pass/fail/parse-error each direction).
**Depends on:** Phase 5 (executor), Phase 6 (image fixture).

---

### Step 9.6 — Local suggestions & feedback

**Goal:** hosted-loop UX polish, BYOK-owned.

**How to implement:**

1. `ByokSuggestions.js`: after a chat goes `ready`, one `fast`-profile call
   ("suggest 3 short follow-ups given this transcript tail") → chips through
   the existing `message.suggestions` rendering (server chats already show
   them; BYOK transcript items simply start carrying the field). Off by
   default behind a settings toggle (token spend is the user's).
2. Feedback: thumbs up/down on BYOK messages store `{chatId, messageId,
   rating, timestamp}` locally (capped 500) — a settings "export feedback
   JSON" button exists for prompt iteration; nothing is sent anywhere.

**Tests:** suggestion shape matches the UI's expectations; toggle off ⇒ no
call; feedback store cap.
**Depends on:** 9.4.

---

### Step 9.7 — Retry & robustness polish

**Goal:** the audit's remaining sharp edges.

**How to implement:**

1. `ByokClient`: persist the degraded `reasoning_effort` outcome into
   `ByokCapabilities` on first failure (no per-turn retry storm); parse and
   honor `Retry-After` (cap 30 s) in the retry backoff; per-request
   `timeoutMs` override from profile (long-reasoning models need >120 s).
2. Orchestrator: refresh the project snapshot **per model call** when the
   previous turn executed edits (finishes the staleness fix; the prompt's
   "may be slightly stale" caveat finally becomes rare truth). Snapshot
   fetch failures degrade to the previous snapshot with a chat-visible
   warning line.
3. Rate-limit UX: a `byok-rate-limited` transient banner on the chat while
   backing off (the error row already exists; this only makes it
   non-terminal-looking when a retry is pending).

**Tests:** retry-after honored (fake timers); degraded-effort remembered
(second turn sends none); snapshot refresh condition (edited ⇒ refetched);
timeout override plumbed.
**Depends on:** Phase 5.

---

### Step 9.8 — Dev-only eval harness

**Goal:** measure before/after for every future prompt/skill/tool change.

**How to implement:**

1. `scripts\run-byok-evals.js` + `Byok\evals\`: ~30 tasks in the SOTA
   recommended shape (real-project grounding, multi-tool, verifiable):
   10 event-logic ("coin spawner every 3 s"), 5 layout/spatial ("place 5×5
   grid of X with even spacing" — scored via instance positions), 5
   variables/properties, 5 JS/extension (Phase 8 surface), 5
   perception-repair ("the preview screenshot shows overlapping HUD — fix").
2. Scoring: mechanical where possible (project-diff vs reference, instance
   geometry, test-pass), LLM-as-judge optional (runs on the user's endpoint,
   flagged as such in the report). Output: per-task pass/fail, rounds, tool
   calls, tokens — the four numbers SOTA says to track.
3. Runner lives outside the shipped bundle (script + fixtures excluded from
   the app build; `Byok\evals\` marked dev-only in the build config check).
   Results are markdown reports committed under `REVIEW\evals\` for trend
   tracking.

**Tests:** harness self-check — a trivially-passing and trivially-failing
task both score correctly.
**Depends on:** everything it evaluates (grow the suite as phases land).

---

### Step 9.9 — Phase gate

1. Full gates from `newIDE\app`.
2. Manual QA (recorded): 40+-round build session survives via compaction
   (watch the "Context summarized" row appear and the loop continue);
   restart the app mid-history → chats reload with images; the F2 pair
   works (progress sentences per completed to-do, and a stall notice —
   in-chat, once per window — after parking the endpoint mid-turn);
   multi-provider routing exercised (register two providers, per-chat
   dropdown overrides, network tab shows different models per call kind;
   badge + token row visible); benchmark run on two models produces a
   sensible ranking; regression — hosted AI and all Phase 5–8 scenarios
   still green.
3. Prompt version bump only if behavior text changed (`byok-v7`).

---

## 3. Phase 9 acceptance criteria (phase gate)

- [ ] F2 runtime half: the watchdog fires once per stall window, in-chat, and disarms on activity/stop (unit-tested + QA); the Phase 7 prompt half shows per-to-do progress sentences (QA).
- [ ] A 40-round session crosses the 0.75 threshold, compacts with the preserved block intact, and completes — no `byok-context-full` unless compaction cannot fit (unit + QA).
- [ ] Chats, images, and archive state survive app restart; quota eviction touches images before transcripts (unit-tested).
- [ ] F3: providers register with isolated keys; the chat model/effort dropdowns resolve per-chat > policy > global; fast/strong routing per call kind; unset advanced fields omitted (all unit-tested); the D5 badge + exact token row is visible (QA).
- [ ] Capability cache eliminates repeated reasoning_effort 400s (unit-tested); benchmark runs and ranks models on the user's machine.
- [ ] Suggestions (opt-in) and local feedback work; nothing is sent to GDevelop servers.
- [ ] Eval harness runs the suite and writes a markdown report under `REVIEW\evals\`.
- [ ] All four checks green; worklog entry complete.

---

## 4. Deferred (post-9 backlog)

- **Token streaming (D2)** — rejected for now (decision #2, 2026-09-22);
  replaced by F2 (Phase 7 step 7.1 + Phase 9 step 9.1). Revisit only if
  perceived latency is still a complaint after F2 lands (also tracked in
  `deferred.md`); the old streaming step design (SSE +
  `stream_options: {include_usage}`, mid-stream Stop) is preserved in
  this file's git history.
- **Char-based token estimate for usage-less endpoints (the O2
  fallback)** — accepted as-is (decision #12); build only if a real
  endpoint in use proves to never send `usage` (also tracked in
  `deferred.md`).
- Server-sent context caching / prompt-prefix caching exploitation (provider-
  specific; revisit when OpenAI-compatible caches are common on user
  endpoints).
- Embedding-based memory (mem0-style extract/update) for cross-chat project
  memory — project notes cover v1 needs.
- Multi-endpoint fan-out (different providers per profile is already
  possible; per-tool endpoints is overkill until asked for).
- Telemetry/quality opt-in sharing — privacy-sensitive, needs explicit user
  program; local feedback export is the v1 ceiling.
